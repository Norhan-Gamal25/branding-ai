# Copy this to .env.local for local development.
# For Vercel deployment, set these in vercel.json or the Vercel dashboard.

# URL of the FastAPI backend — used by the Next.js proxy at /api/backend
# and directly by the browser for loading static image files.
BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
